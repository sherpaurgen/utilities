### Gtail
A custom go commandline tool to monitor/read a dynamically updated(append) event files, forwards the new events to remote target's tcp/udp port.

- Support TLS encryption


```
gtail /var/log/syslog
```
